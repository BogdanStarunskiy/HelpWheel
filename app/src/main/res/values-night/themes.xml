<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.HelpWheel" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/white</item>
        <item name="colorPrimaryVariant">@color/dark</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/dark</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <item name="bottomSheetDialogTheme">@style/AppBottomSheetDialogTheme</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="WelcomeAlertDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>

    <style name="AppBottomSheetDialogTheme" parent="Theme.MaterialComponents.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/CustomBottomSheetStyle</item>
    </style>

    <style name="CustomBottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@drawable/drawable_round_bottom_sheet_night</item>
    </style>

    <style name="WelcomeDialogTextStyle" parent="Theme.HelpWheel">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="WelcomeDialogSubTextStyle" parent="Theme.HelpWheel">
        <item name="android:textColor">@color/gray</item>
    </style>

    <style name="FuelAlertDialogStyle" parent="Theme.HelpWheel">
        <item name="android:backgroundTint">@color/white</item>
    </style>

    <style name="FuelAlertDialogButtonStyle" parent="FuelAlertDialogStyle">
        <item name="android:textColor">@color/dark</item>
    </style>

    <style name="FuelAlertDialogTextStyle" parent="Theme.HelpWheel">
        <item name="android:textColor">@color/dark</item>
    </style>

</resources>